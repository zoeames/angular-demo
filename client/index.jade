doctype html
html(ng-app='demo')
  head
    title Angular Demo
    link(rel='stylesheet', href='/index.css')
  body(ng-controller='MainController')
    h1 Angular Demo
    input(type='text', ng-model='name', required, ng-minlength='3', ng-maxlength='10', ng-pattern='/groot/')
    h2 I AM {{name}}
    h3 My age is {{age}}

    input(type='checkbox', ng-model='isBirthday')
    h4 isBirthday: {{isBirthday}}
    h1(ng-show='isBirthday') Happy Birthday {{name}}
    ul
      li(ng-repeat='dog in dogs') {{dog.name}} is {{dog.age}} years old.

    pre(ng-hide='isBirthday') {{dogs | json}}

    script(src='/vendor/angular/angular.js')
    script(src='/index.js')
